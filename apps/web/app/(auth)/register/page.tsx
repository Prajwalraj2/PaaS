import { RegisterForm } from '@/components/auth/register-form';

export const metadata = {
  title: 'Register - PaaS Platform',
  description: 'Create your PaaS account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
